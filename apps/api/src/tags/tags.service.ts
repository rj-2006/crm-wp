import { Injectable } from '@nestjs/common'; import { PrismaService } from '../prisma/prisma.service'; @Injectable() export class TagsService { constructor(private db: PrismaService) { } list(companyId: string) { return this.db.tag.findMany({ where: { companyId }, orderBy: { name: 'asc' } }) } create(companyId: string, b: any) { return this.db.tag.create({ data: { companyId, name: b.name, color: b.color } }) } segment(companyId: string, definition: any) { const where: any = { companyId }; if (definition.status) where.status = definition.status; if (definition.tagId) where.tags = { some: { tagId: definition.tagId } }; return this.db.contact.findMany({ where, include: { tags: { include: { tag: true } } } }) } }
