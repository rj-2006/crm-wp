import { Body, Controller, Get, Post, Query, Req, Param, UseGuards } from '@nestjs/common'; import { WhatsAppService } from './whatsapp.service'; import { AuthGuard } from '../common/auth.guard'; @Controller('whatsapp') export class WhatsAppController { constructor(private s: WhatsAppService) { } @Get('webhook') verify(@Query('hub.mode') mode: string, @Query('hub.verify_token') token: string, @Query('hub.challenge') challenge: string) { if (mode === 'subscribe' && token === (process.env.META_VERIFY_TOKEN || 'change-me')) return challenge; return 'forbidden' } @Post('webhook') webhook(@Body() b: any) { return this.s.webhook({}, b) } @UseGuards(AuthGuard) @Get('accounts') accounts(@Req() r: any) { return this.s.accounts(r.user.companyId) } @UseGuards(AuthGuard) @Post('accounts') create(@Req() r: any, @Body() b: any) { return this.s.createAccount(r.user.companyId, b) } @UseGuards(AuthGuard) @Post('messages') send(@Req() r: any, @Body() b: any) { return this.s.send(r.user.companyId, b) } @UseGuards(AuthGuard) @Get('messages/:contactId') history(@Req() r: any, @Param('contactId') id: string) { return this.s.history(r.user.companyId, id) } }
